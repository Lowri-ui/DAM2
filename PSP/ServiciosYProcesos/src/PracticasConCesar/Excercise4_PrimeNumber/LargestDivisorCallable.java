package PracticasConCesar.Excercise4_PrimeNumber;

import java.util.concurrent.Callable;

public class LargestDivisorCallable implements Callable<Integer> {
    @Override
    public Integer call() throws Exception {
        return 0;
    }
}
